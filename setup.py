#!/usr/bin/env python

from distutils.core import setup

setup(name='storeback',
      version='0.0.1',
      description='Store for prony.',
      author='promisejohn',
      author_email='promise.john@gmail.com',
      url='http://github.com/promisejohn/storeback',
      packages=['src/prony'],
      # py_modules=['app']
      # install_requires = [],
      # dependency_links = []
      )
